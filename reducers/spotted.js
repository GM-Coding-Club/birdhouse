
import { } from '../actions/'

let initialState = {
  visible: false,
  bird: undefined
}

const spottedReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
 
export default spottedReducer