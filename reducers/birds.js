
//import { } from '../actions/'

let initialState = []

const birdsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
 
export default birdsReducer