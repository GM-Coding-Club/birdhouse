
import { } from '../actions/'

let initialState = []

const cashReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
 
export default cashReducer